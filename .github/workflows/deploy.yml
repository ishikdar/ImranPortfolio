name: Deploy Portfolio to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Pages
        uses: actions/configure-pages@v4
        
      - name: Verify source files exist
        run: |
          echo "Checking source files..."
          ls -la site/public/pages/
          ls -la site/public/css/
          ls -la site/public/assets/ || echo "Assets directory not found"
          ls -la site/public/js/
          ls -la site/public/data/
          
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: Install dependencies and build TypeScript
        run: |
          npm install
          npm run build
          echo "TypeScript compiled successfully"
          ls -la site/distribution/
          
      - name: Copy files to build directory
        run: |
          mkdir -p _site
          echo "Copying HTML pages..."
          cp -r site/public/pages/* _site/
          echo "Copying CSS..."
          cp -r site/public/css _site/
          echo "Copying JS..."
          cp -r site/public/js _site/
          echo "Copying data..."
          cp -r site/public/data _site/
          echo "Copying compiled TypeScript (distribution)..."
          cp -r site/distribution _site/
          echo "Copying assets..."
          if [ -d "site/public/assets" ]; then
            cp -r site/public/assets _site/
          else
            mkdir -p _site/assets
            echo "Assets directory created"
          fi
          
      - name: Fix asset paths in HTML files
        run: |
          echo "Fixing paths in HTML files..."
          echo "HTML files found:"
          find _site -name "*.html" -type f
          echo "Before path fixing - sample from home.html:"
          if [ -f "_site/home.html" ]; then
            grep -n "src=" _site/home.html | head -3
          fi
          # Update CSS paths from ../css/ to css/
          find _site -name "*.html" -type f -exec sed -i 's|../css/|css/|g' {} \;
          # Update asset paths from ../assets/ to assets/
          find _site -name "*.html" -type f -exec sed -i 's|../assets/|assets/|g' {} \;
          # Update JS paths from ../js/ to js/
          find _site -name "*.html" -type f -exec sed -i 's|../js/|js/|g' {} \;
          # Fix TypeScript distribution path from ../../distribution/ to distribution/
          find _site -name "*.html" -type f -exec sed -i 's|../../distribution/|distribution/|g' {} \;
          echo "After path fixing - sample from home.html:"
          if [ -f "_site/home.html" ]; then
            grep -n "src=" _site/home.html | head -3
          fi
          
      - name: List final build directory
        run: |
          echo "Final build directory contents:"
          ls -la _site/
          echo "HTML files:"
          ls -la _site/*.html
          
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
